# assuredefi.com — closure archive

Static page served by GitHub Pages. `index.html` is generated by `build.cjs` from `inputs/`
(the final public dataset captured 2026-09-10 plus the file lists of the three archive repos).
To change the closure statement, edit the marked EDIT-BLOCK in `index.html` (or `build.cjs` and rerun).
